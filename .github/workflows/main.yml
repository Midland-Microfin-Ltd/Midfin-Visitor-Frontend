name: Build and Push Frontend Image

on:
  push:
    branches:
      - dev
      - staging
      - main

jobs:
  build-react-app:
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: 22.x

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Build React App (Validation)
        run: npm run build


  build-docker-image:
    runs-on: ubuntu-latest
    needs: build-react-app
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: techlead-midland
          password: ${{ secrets.GHCR_PAT }}

      - name: Determine Environment
        run: |
          BRANCH="${GITHUB_REF##*/}"

          if [[ "$BRANCH" == "dev" || "$BRANCH" == "staging" ]]; then
            ENV="uat"
          else
            ENV="prod"
          fi

          echo "ENV=$ENV" >> $GITHUB_ENV
          echo "Using environment: $ENV"

      - name: Set Frontend Environment Variables
        run: |
          if [[ "$ENV" == "uat" ]]; then
            echo "VITE_ENVIRONMENT=development" >> $GITHUB_ENV
            echo "VITE_HOST_DOMAIN=midfinvisitoruat.midlandmicrofin.co.in" >> $GITHUB_ENV
            echo "VITE_API_TIMEOUT=30000" >> $GITHUB_ENV
          else
            echo "VITE_ENVIRONMENT=production" >> $GITHUB_ENV
            echo "VITE_HOST_DOMAIN=midfinvisitor.midlandmicrofin.co.in" >> $GITHUB_ENV
            echo "VITE_API_TIMEOUT=30000" >> $GITHUB_ENV
          fi

      - name: Build and Push Docker Image
        run: |
          IMAGE="ghcr.io/techlead-midland/midland-microfin-ltd/visitor-management-frontend:${ENV}"

          echo "Building image: $IMAGE"

          docker build \
            --build-arg VITE_ENVIRONMENT=${VITE_ENVIRONMENT} \
            --build-arg VITE_HOST_DOMAIN=${VITE_HOST_DOMAIN} \
            --build-arg VITE_API_TIMEOUT=${VITE_API_TIMEOUT} \
            -t "$IMAGE" .

          docker push "$IMAGE"